let navigation = null;
const utils = {
    setNavigation: (_navigation) => {
        navigation = _navigation;
    },
    getNavigation: () => {
        return navigation
    },
}

export default utils